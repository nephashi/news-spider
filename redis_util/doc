当前的url字典结构：
news_dic = {
               'title': news.css('::text').extract_first(),
               'link': news.css('::attr(href)').extract_first()
           }

当前新闻数据结构是一个字典：
dict = {
           'title': titles,
           'content': content
       }